export enum SubscriptionPlatform {
  ANDROID = 'android',
  IOS = 'ios',
}

export enum SubscriptionStatus {
  ACTIVE = 'active',
  EXPIRED = 'expired',
  CANCELED = 'canceled',
  PENDING = 'pending',
}
